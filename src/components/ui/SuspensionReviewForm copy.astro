<form id="suspension-review-form" class="space-y-6">

  <!-- Name + Email -->
  <div class="grid md:grid-cols-2 gap-5">

    <div>
      <label class="block text-sm text-white/70 mb-2">
        Name *
      </label>

      <input
        type="text"
        name="name"
        required
        class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white placeholder-white/40 transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
      />
    </div>

    <div>
      <label class="block text-sm text-white/70 mb-2">
        Email *
      </label>

      <input
        type="email"
        name="email"
        required
        class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white placeholder-white/40 transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
      />
    </div>

  </div>

  <!-- Ads ID + Domain -->
  <div class="grid md:grid-cols-2 gap-5">

    <div>
      <label class="block text-sm text-white/70 mb-2">
        Google Ads Customer ID *
      </label>

      <input
        type="text"
        name="adsId"
        placeholder="123-456-7890"
        required
        class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white placeholder-white/40 transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
      />
    </div>

    <div>
      <label class="block text-sm text-white/70 mb-2">
        Affected Domain(s) *
      </label>

      <input
        type="text"
        name="domain"
        placeholder="example.com"
        required
        class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white placeholder-white/40 transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
      />
    </div>

  </div>

  <!-- Service Issue -->

  <div>
    <label class="block text-sm text-white/70 mb-2">
      Service / Issue *
    </label>

    <select
      name="issue"
      required
      class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
    >
      <option value="">Select issue</option>
      <option>Circumventing Systems Policy</option>
      <option>Suspicious Payments Policy</option>
      <option>Unacceptable Business Practices Policy</option>
      <option>Business / Identity Verification Issues</option>
      <option>Merchant Center Suspension</option>
      <option>Multiple / Repeat Suspensions</option>
      <option>Not Sure</option>
    </select>
  </div>

  <!-- Contact -->

  <div>
    <label class="block text-sm text-white/70 mb-2">
      Contact Number (optional)
    </label>

    <input
      type="tel"
      name="phone"
      class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white placeholder-white/40 transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
    />
  </div>

  <!-- Description -->

  <div>
    <label class="block text-sm text-white/70 mb-2">
      Describe the issue *
    </label>

    <textarea
      name="description"
      rows="4"
      required
      class="w-full rounded-xl bg-[#061322] border border-white/10 px-4 py-3 text-white placeholder-white/40 transition-all duration-200 focus:border-[#3BA6FF]/60 focus:ring-2 focus:ring-[#3BA6FF]/25 focus:outline-none"
    ></textarea>
  </div>

  <!-- Budget -->

  <fieldset>
    <legend class="block text-sm text-white/70 mb-3">
      Monthly Google Ads Budget *
    </legend>

    <div class="space-y-2 text-white/80">

      <label class="flex items-center gap-2">
        <input type="radio" name="budget" value="under-1000" required class="accent-[#3BA6FF]" />
        Under £1,000
      </label>

      <label class="flex items-center gap-2">
        <input type="radio" name="budget" value="1000-5000" class="accent-[#3BA6FF]" />
        £1,000 – £5,000
      </label>

      <label class="flex items-center gap-2">
        <input type="radio" name="budget" value="5000-20000" class="accent-[#3BA6FF]" />
        £5,000 – £20,000
      </label>

      <label class="flex items-center gap-2">
        <input type="radio" name="budget" value="20000+" class="accent-[#3BA6FF]" />
        £20,000+
      </label>

    </div>

  </fieldset>

  <!-- Agreement -->

  <div class="flex items-start gap-3 text-sm text-white/70">

    <input
      type="checkbox"
      name="agreement"
      required
      class="accent-[#3BA6FF] mt-[2px]"
    />

    <p>
      I understand that a live screen share is required for the eligibility
      assessment.
    </p>

  </div>

  <!-- Submit -->

  <button
    type="submit"
    class="w-full mt-4 rounded-xl bg-gradient-to-r from-[#3BA6FF] to-[#60C3FF] py-4 text-white font-semibold transition-all duration-200 hover:brightness-110 hover:shadow-[0_8px_30px_rgba(59,166,255,0.35)]"
  >
    Continue to Schedule
  </button>

</form>

<script>
document.addEventListener("DOMContentLoaded", () => {

  const form = document.getElementById("suspension-review-form") as HTMLFormElement;
  if (!form) return;

  form.addEventListener("submit", function(e) {
    e.preventDefault();

    const formData = new FormData(form);

    const name = String(formData.get("name") || "");
    const email = String(formData.get("email") || "");
    const adsId = String(formData.get("adsId") || "");
    const domain = String(formData.get("domain") || "");
    const issue = String(formData.get("issue") || "");
    const phone = String(formData.get("phone") || "");
    const description = String(formData.get("description") || "");
    const budget = String(formData.get("budget") || "");

    const baseUrl = "https://calendly.com/gianluca-adrestore/30min-consultation";

    const calendlyUrl =
      baseUrl +
      "?name=" + encodeURIComponent(name) +
      "&email=" + encodeURIComponent(email) +
      "&a1=" + encodeURIComponent(adsId) +
      "&a2=" + encodeURIComponent(domain) +
      "&a3=" + encodeURIComponent(issue) +
      "&a4=" + encodeURIComponent(phone) +
      "&a5=" + encodeURIComponent(description) +
      "&a6=" + encodeURIComponent(budget);

    window.location.href = calendlyUrl;

  });

});
</script>